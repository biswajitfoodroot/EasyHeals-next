# Skip Playwright browser binary download during npm install.
# Playwright is a devDependency used only locally for browser automation testing.
# On Vercel, devDependencies are excluded from production builds.
# This guard prevents accidental binary downloads if the install is ever run without --omit=dev.
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
