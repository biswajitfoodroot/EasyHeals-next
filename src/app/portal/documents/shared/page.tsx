import SharedDocsClient from "./SharedDocsClient";

export const metadata = { title: "Shared Patient Records | EasyHeals Portal" };

export default function SharedDocsPage() {
  return <SharedDocsClient />;
}
